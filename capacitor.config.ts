import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'clase-25-sept-2021',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
